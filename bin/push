#!/usr/bin/env bash
pname="$(./bin/getprojectname)"
echo "[+] pushing source to scratch org $pname"
sfdx force:source:push -w 10 -u "$pname" && wait
echo "[+] finished pushing source to scratch org $pname"
